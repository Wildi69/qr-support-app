# === Admin login ===
ADMIN_USER=admin@example.com
ADMIN_PASS_HASH=changeme

# === Application secrets ===
SECRET_KEY=changeme
SESSION_SECRET=changeme

# === Database ===
DATABASE_URL=sqlite:///dev.db

# === Cookie/session options ===
COOKIE_SECURE=false
COOKIE_SAMESITE=lax
SESSION_COOKIE_NAME=qr_session
FLASH_COOKIE_NAME=qr_flash
SESSION_MAX_AGE_MIN=120

# === Email (Phase 5) ===
EMAIL_ENABLED=false
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_STARTTLS=true
MAIL_FROM="Support <support@example.com>"
